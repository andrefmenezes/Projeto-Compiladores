'use strict'

const inputs = `
table(
  id=newBtn1,
  class=newClass1,
    #tr(
      id=newBtn,
      class=newClass,
      #td(
        id = teste1,
        class=newClass2
      ),
      #td(
        id = teste2,
        class=newClass2
      )
    )
)
`;

export default inputs;